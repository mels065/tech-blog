<div id="dashboard">
    <ul id="my-posts">
        {{#each posts as |post|}}
        {{> dashboard-post}}
        {{/each}}
    </ul>

    <a id="create-post-link" class="btn btn-primary" href="/post">Create Post +</a>
</div>
